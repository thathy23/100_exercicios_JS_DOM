/**24 - Crie um script que declare um array de strings e utilize a função join() para concatenar todas as strings
 *  em uma única string, separadas por vírgulas. Imprima o resultado. */

const nome = ["Tatiane", "de Araújo", "Fontes"];

const nomes = nome.join();

const nomeCompleto = nome.join(' ');

console.log (nomes);
console.log (nomeCompleto);

